// Generated by CoffeeScript 1.3.3
(function() {
  var chai, expect, nt, should,
    _this = this;

  chai = require('chai');

  should = chai.should();

  expect = chai.expect;

  nt = require('../noxious/noxious_types.js');

  describe('TextField', function() {
    describe('.constructor(length)', function() {
      return it('should ', function(done) {
        var m;
        m = new nt.TextField(10);
        m.length.should.equal(10);
        m.default_value.should.equal('');
        return done();
      });
    });
    return describe('.constructor(length,default_value)', function() {
      return it('should ', function(done) {
        var m;
        m = new nt.TextField(10, 'test');
        m.length.should.equal(10);
        m.default_value.should.equal('test');
        return done();
      });
    });
  });

  describe('ArrayField', function() {
    return describe('.constructor(item_template_name)', function() {
      return it('should ', function(done) {
        var m;
        m = new nt.ArrayField('template');
        m.item_template_name.should.equal('template');
        expect(m.default_value).to.be.a('Array');
        return done();
      });
    });
  });

  describe('RefField', function() {
    return describe('.constructor(item_template_name)', function() {
      return it('should ', function(done) {
        var m;
        m = new nt.RefField('template');
        m.item_template_name.should.equal('template');
        expect(m.default_value).to.be.a('Object');
        return done();
      });
    });
  });

}).call(this);
